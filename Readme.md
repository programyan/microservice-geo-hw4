### Ads

This is a second homework of the "Microservices with Ruby" course.

## Basic Usage

```shell
$ docker-compose up -d
$ bin/setup
$ bin/server
```

## Testing

```shell
$ docker-compose up -d
$ RAILS_ENV=test bin/setup
$ rspec
```

